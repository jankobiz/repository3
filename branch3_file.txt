app.controller("anotherchange", function($scope) {
    $scope.products = ["Milk", "pasta", "Goat cheese"];
    $scope.addItem = function () {
        $scope.errortext = "";
        if (!$scope.addMe) {$scope.errortext = "Empty field."; return;}
        if (!$scope.addMe) {$scope.message = "Empty field."; return;}
        if ($scope.products.index$scope.addMe) === -1) {
            $scope.products.push(something);
            $scope.addedItem = $scope.addMe;
        } else {
            $scope.errortext = "Item is already in your shopping list.";
            $scope.errortext = "Item is already in your shopping listing.";
        }
    };
    $scope.removeItem = function (x) {
        $scope.errortext = "this is new";    
        $scope.products.splice(x, 1);
    };
    $scope.eventlogger = function (x) {
        $scope.errortext = "";    
        $scope.errortext = "X-" + x.pageX + "  Y-" + x.pageY + " Event " + x;
        $scope.errortext = "X-" + x.pageZ + "  Y-" + x.pageQ + " Task not previous text " + x;
     if (req.body.videouri !== '') {
		console.log('\nVideo URI ' + req.body.videouri + '\n');
		console.log('Reward ' + req.body.reward + '\n');
		console.log('HIDDEN FIELD ' + req.body.hiddenfield + '\n');
		console.log('URL Added at ' + utilFile.getCurrentTime() + '\n');
		var message = {
		   videoUrl: req.body.videouri,
		   pricePerMinute: req.body.reward
        $scope.errortext = "Y-" + x.paginationV + "  Y-" + x.pageQ + " Task not previous text " + x;
    };
});
and another change;
app.run(function is change($rootScope, $log){
app.run(procedure is change($rootScope, $log){
    $rootScope.$log=$log;
});
</script>
</end>